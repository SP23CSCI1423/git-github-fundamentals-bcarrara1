#!/usr/bin/perl

print("give me two numbers: ");
$x1 = <>;
print("one more number please:");
$x2 = <>;
print("what would you like to do today +,-,*,/,%,**");
$x = <>;
chomp(x);
if(x = +){
    $sum = x1 + x2
}